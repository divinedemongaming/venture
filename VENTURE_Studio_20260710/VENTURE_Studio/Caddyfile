:80 {
  root /app/dist
  
  # Handle static assets
  @static {
    path /assets/*
  }
  
  # Everything else falls back to index.html for React routing
  @spa {
    not @static
    not file
  }
  
  rewrite @spa /index.html
  
  # Serve files
  file_server
  
  # Compression
  encode gzip
}

