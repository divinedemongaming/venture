:{$PORT:3000} {
  root /app/dist
  
  # Serve index.html for SPA routing
  @notStatic {
    not path /assets/*
    not file
  }
  
  rewrite @notStatic /index.html
  
  # Serve files
  file_server
  
  # Compression
  encode gzip
}

