:80 {
  root /app/dist
  
  # SPA routing — fallback to index.html for all 404s
  try_files {path} /index.html

  # Serve static assets with caching
  file_server {
    hide .env .env.* .git* .DS_Store
  }

  # Compression
  encode gzip

  # Log
  log {
    level warn
  }
}

